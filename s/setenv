#keep chmod settings
git config --global core.fileMode false
#leave as is on Mac/Linux
git config --global core.autocrlf input

# change this to your name
git config user.email "koliapp2019@gmail.com"
git config user.name "KOLI DEV"

# change this to your ANDROID directory
export ANDROID_HOME=~/home/spatel/Android/Sdk
export PATH=$PATH:$ANDROID_HOME/platform-tools
export PATH=$PATH:$ANDROID_HOME/platform-tools
#use /usr/libexec/java_home -v to view the Java home
# change this to your JDK directory
export JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64/Contents/Home